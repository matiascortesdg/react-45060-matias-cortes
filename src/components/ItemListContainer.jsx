//Creamos el componente ItemListContainer.jsx

const ItemListContainer = ( {greeting} ) => {
    
    return (
        
        <h1 className="h1-title">Hola! {greeting}</h1>

    ); 
};

export default ItemListContainer;